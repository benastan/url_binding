- stories.each do |story|
  %div
    .story{ data: { bind_url: "/stories/#{story[:id]}" } }
      = haml :story, locals: { story: story }

    .edit-story{ data: { bind_url: "/stories/#{story[:id]}/edit" } }
